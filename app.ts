"use strict";

/*~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~*\
|       Solves Sudoku puzzles (duh)        |
| https://github.com/Lamby777/sudoku-solve |
\*~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~*/

// dumbass requirement for top-level await
export {}

const puzzle = await readPuzzle();

async function readPuzzle() {
	//fetch("./file.json").then(res => json = res.json())
}
